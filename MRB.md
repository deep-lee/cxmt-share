我在半导体DRAM公司的QRA部门工作，目前我负责的一件事，是整个多个部门的MRB模板，然后以在线文档的形式每个MRB case建立一个文档，文档会包含case基本信息，问题描述，Impact Scope和Disposition等部分，每个部分都是用表格的形式来填写的。然后我们现在要做的一件事是用AI对每个case按照某个特定的格式对case内容进行总结，然后作为周报发给部门一级部门老板。然后我现在的问题是，因为每个case它会在不断的去更新的，比如root cause，risk assessment，disposition等等，那我在文档中可以用怎样的形式去体现这个更新，这样我周报总结的时候，我就可以知道我这一周这个case有哪些更新，在最终总结的内容中对更新的部门高亮展示。

备注：这个在线文档你可以看作是markdown格式。


处理跨部门的 MRB (Material Review Board) case 并为一级部门负责人自动生成带有增量更新的周报，是一个非常有价值但又充满挑战的工程。在快速迭代的质量管理中，Root Cause 和 Disposition 随时都在变，单纯依赖大模型去“猜”哪里更新了往往不够精准。

要在 Markdown 格式的在线文档中体现更新，并且让 AI 能够精准抓取、在最终周报中高亮展示，你有以下几种不同维度的落地思路。可以根据你们团队成员的文档填写习惯以及你这边的开发成本来选择：

### 方案一：文档规范约定（依赖业务人员手动标记）

这是最轻量级的做法，不需要太多额外的开发，只需要规范大家填写文档的格式。大模型对结构化的标记非常敏感。

**1. 单元格内嵌时间戳标签 (Inline Tags)**
在 Markdown 表格的单元格内，每次有新进展时，要求填写人使用特定的格式前缀。例如使用 `**[YYYY-MM-DD 更新]**` 或 `> W10 Update:`。

* **示例：**
```markdown
| 项目 | 内容 |
| :--- | :--- |
| Root Cause | 初始排查：设备 A 参数异常。<br> **[2026-03-05 更新]：** 确认是 Sensor 灵敏度漂移导致。 |
| Disposition | 隔离相关批次。<br> **[2026-03-04 更新]：** 报废 2 个 Lot，其余降级 (Downgrade) 处理。 |

```


* **AI 提取策略：** 在 Prompt 中明确指示 AI：“查找所有带有 `[YYYY-MM-DD 更新]` 标签的文本。提取本周（如 3月2日-3月8日）的内容作为本周进展，并在生成摘要时使用 `<mark>更新内容</mark>` 或 `**更新内容**` 进行高亮。”

**2. 增加专属的“周次更新区” (Changelog Section)**
不要让大家在原来的表格里反复修改，而是在文档末尾维护一个类似于系统更新日志的 Markdown 表格。

* **示例：**
```markdown
### 更新日志 (Update History)
| 日期 | 更新模块 | 更新内容摘要 | 填写人 |
| :--- | :--- | :--- | :--- |
| 2026-03-05 | Root Cause | 确认 Sensor 灵敏度漂移 | 张三 |
| 2026-03-05 | Disposition | 决定报废 2 个 Lot | 李四 |

```


* **AI 提取策略：** 这种结构对 RAG 或文档解析非常友好。AI 只需要提取本周日期的行，然后将其与上方的主表格内容结合起来生成带有高亮的总结。

---

### 方案二：工程化手段（后端 Diff + AI 总结，最推荐）

从技术负责人的视角来看，单纯依靠跨部门的业务人员去严格遵守 Markdown 标记规范，长期来看容错率较低（总有人会忘记写日期或者写错格式）。更健壮的做法是将“寻找差异”的工作交给系统。

既然是在线文档，通常会有版本历史或 API 可以拉取数据。

**1. 快照对比法 (Snapshot Diffing)**

* **逻辑：** 你的后台调度任务每周五拉取一次当前文档的 Markdown 内容（Current_MD），并从数据库或缓存中读取上周五该文档的快照内容（LastWeek_MD）。
* **流程：** 将这两份文本发给大模型（现在主流模型的上下文窗口完全足够容纳两个版本的 Markdown 表格）。
* **Prompt 示例：**
```text
你是一个半导体制造企业的资深 QRA 专家。
这里有一份 MRB Case 的上周文档状态：
<last_week> {LastWeek_MD} </last_week>

这里是本周该 Case 的最新文档状态：
<this_week> {Current_MD} </this_week>

请你对比两份文档，总结当前该 Case 的整体情况，并严格按照以下格式输出：
1. 必须精准识别出本周相比上周发生改变的内容（如 Root Cause、Impact Scope 等的变动）。
2. 在生成的总结中，必须将本周新增或修改的内容使用 Markdown 的加粗和颜色标签进行高亮，例如：**<font color="red">新增：报废 2 个 Lot</font>**。

```



**2. AST/结构化数据 Diff**
如果你这边的后端系统在提取 Markdown 时将其转换为了结构化数据（比如 JSON），你可以直接在 Java 代码层面对比出差异。

* 例如，对比发现 JSON 中的 `Disposition` 字段发生了变化。
* 你可以将这个精确的变动点组装成一段提示词传给 AI：“本周 `Disposition` 从 '隔离待查' 更新为了 '报废 2 Lot，其余 Downgrade'，请基于此生成周报摘要并高亮这句话。” 这种方式对幻觉的控制是最好的。

---

### 给老板的呈现建议

一级部门老板在看周报时，通常关注的是 **进度** 和 **风险**。你可以在最终大模型生成的汇总模板里采用这种高亮结构：

> **MRB Case-20260305: [XXX缺陷]**
> * **基本信息:** 发起部门 XXX, 影响范围 5 个 Lot。
> * **本周最新进展:** **[本周更新] 最终确认 Root Cause 为机台 Sensor 故障，已将 2 个受影响 Lot 执行报废处理。**
> * **下一步计划:** 监控后续 3 批次良率 (OEE 监控中)。
> 
> 

### 下一步建议

在这几种方案中，你更倾向于通过**规范人员填写格式**来解决，还是通过**后端代码拉取两个版本的历史做自动对比**来解决？如果你考虑结合代码对比，我们可以进一步聊聊如何利用你熟悉的框架（如 Spring Boot 结合 LangChain/相关 AI SDK）来构建这个解析与处理的 Pipeline。



我更倾向于规范人员填写格式来解决。然后还有一个问题是，如果对某一个内容有修正，比如Excursion Level，可能一开始是TBD，然后更新为Minor，那我是不是约束在修正内容的时候，将旧的值划线，然后使用[YYYY-MM-DD 更新]**更新为新的值，这样AI能不能识别出来。



**完全可以，而且这是一个非常优秀的做法。**

大语言模型对 Markdown 语法（如 `~~删除线~~`、`**加粗**`）以及时间标签（如 `[YYYY-MM-DD]`）非常敏感，这种结构化的文本可以说是最受 AI 欢迎的输入格式之一。

当你使用 `~~TBD~~ **[2026-03-05 更新] Minor**` 这种格式时，AI 会在底层建立非常清晰的逻辑映射：

1. **`~~TBD~~`**：这部分信息已被废弃或被覆盖。
2. **`[2026-03-05 更新]`**：这是一个明确的时间戳，触发了“状态变更”的事件。
3. **`Minor`**：这是该字段当前最新的有效值。

为了确保 AI 在生成周报时能百分之百精准地抓取这些修正内容，并完美呈现给一级部门老板，建议你在推行这个规范和编写 AI Prompt（提示词）时，配合以下几个策略：

### 1. 制定清晰的填写规范 (SOP 示例)

你可以直接把下面这个规范发给各部门的填写人，明确要求：

* **新增内容**：直接在原有内容后追加，使用格式 `**[YYYY-MM-DD 更新] 新内容**`。
* **修正内容**：对旧值使用 Markdown 删除线 `~~旧值~~`，紧接着补充新值 `**[YYYY-MM-DD 更新] 新值**`。
* **多次修正**：如果一个字段被反复修改，保留历史痕迹（将之前的更新也划线）。
* *示例*：`~~TBD~~` `~~[2026-03-01 更新] Minor~~` `**[2026-03-05 更新] Major**`



### 2. 为 AI 设计专属的“周报提取 Prompt”

既然你选择了规范格式，那么 AI 的核心工作就变成了“带时间窗口的定向信息提取和对比”。在调用 AI 时，你的 Prompt 需要明确告诉它如何处理这些 Markdown 标记，并且**一定要把“本周的时间范围”作为参数传给 AI**。

你可以参考以下 Prompt 模板：

```text
你是一个专业的半导体 QRA 部门数据分析助手。你的任务是根据提供的 MRB Case Markdown 文档，提取并总结【本周】的最新进展，生成一份给一级部门负责人的周报摘要。

【当前周报时间范围】：2026-03-02 至 2026-03-08

【提取规则】：
1. 重点扫描文档中带有 `[YYYY-MM-DD 更新]` 格式的标签。
2. 只有当日期落在【当前周报时间范围】内时，才将其视为“本周更新”。
3. 识别删除线 `~~文本~~`：如果本周的更新伴随着对旧值的删除线，请在总结中明确指出状态的“变更”。例如，旧值是 `~~TBD~~`，新值是 `[2026-03-05 更新] Minor`，你需要总结为：“将 Excursion Level 从 TBD 更新为了 Minor”。
4. 对于非本周的历史更新（日期不在范围内），只作为当前状态的背景参考，**不要**在周报中作为“本周进展”高亮。

【输出格式要求】：
请遵循以下结构输出，必须使用 Markdown 的高亮语法（如加粗或引用）来突出【本周】发生变动的地方：
- **Case 基本信息**：...
- **当前核心状态**：...
- **🔥 本周最新进展**：
  - (在这里列出本周的具体更新，明确指出从什么修改为了什么，或者新增了什么信息)

```

### 3. 防呆机制 (容错处理)

由于依赖人工手写标记，不可避免会有同事写错日期格式（比如写成 `[03/05 更新]` 或 `[3.5 更新]`），或者忘记打删除线直接覆盖了旧值。

在实际落地时，你可以在 Prompt 中加入一句宽容度指令，增强鲁棒性：

* *"注意：请兼容各种日期书写习惯（如 03/05, 3月5日等），只要语义上属于本周范围，均需提取。如果发现有打上时间戳但没有使用删除线直接改变前后文语境的情况，也请通过上下文逻辑判断是否为状态变更。"*

通过这种“强规范 + 智能识别”的组合，不仅能降低你这边的系统开发成本，生成的 MRB 周报也会极其精准，老板一眼就能看到本周由于 Risk Assessment 或 Disposition 变化带来的关键决策进展。